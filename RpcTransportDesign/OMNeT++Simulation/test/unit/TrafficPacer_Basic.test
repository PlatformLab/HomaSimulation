%description:
Test the constructor and basic grant allocation

%includes:
#include <stdio.h>
#include "transport/TrafficPacer.h"

%activity:
double linkSpeed = 10;
uint32_t maxAllowedBytes = 9700;
TrafficPacer trafficPacer(linkSpeed, maxAllowedBytes);
if (trafficPacer.okToGrant(0, 1500)) {
    EV << "Can grant in zero time";
}

%contains: stdout
Can grant in zero time

